#include <iostream>
#include <queue>

using namespace std;

struct TeacherOP
{
    int num, time;
    bool borrow;
    TeacherOP(int t, int n, bool b) : time(t), num(n), borrow(b) {}
};




int N, K;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin >> N >> K;
    while (K--) {

    }
    return 0;
}
